/target
/result
/result-*
.direnv/

# VM disk images and the darwin.linux-builder keypair, in case anything is run
# from inside the repo. Never commit them — keys/ holds a private key. Unanchored
# so they match wherever they land. Better still, keep these outside the repo
# (see host-setup/linux-builder/README.md and lima/build-prebuilt-image.sh).
keys/
*.qcow2
